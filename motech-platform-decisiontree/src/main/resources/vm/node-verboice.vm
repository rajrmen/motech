<?xml version="1.0" encoding="UTF-8"?>
<Response>
    #foreach( $prompt in $node.prompts )
        #if($prompt.class.simpleName == "AudioPrompt")
            <Play>$prompt.audioFileUrl</Play>
        #else
            <Say>$prompt.message</Say>
        #end
    #end
    #if ($node.transitions.size() > 0)
        #foreach( $mapEntry in $node.transitions.entrySet() )
            #set ($url = "$contentPath/decisiontree/node?type=$type&pId=$patientId&ln=$language&tNm=$treeName&trP=$transitionPath")
            <Gather method="POST" action="$escape.escapeXml($url)"></Gather>
        #end
     #else
    #end
</Response>